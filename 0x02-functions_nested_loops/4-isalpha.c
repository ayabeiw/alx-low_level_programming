#include "holberton.h"
int _isalpha(int c)

/**
* _isalpha - Checks for alphabectic Characters
*@c: letter to be chcked, lowercase or uppercase
* Return: Always 0 (Success)
*/

int _isalpha(int c);

if ((c >= 97 && c <= 122) || (c >= 65 && c <= 90))

{return (1);
else
return (0);
}
