#include "holberton.h"

/**
 * print_alphabet_x10 - Prints 10 the alphabet ten times in lower case
 */
void print_alphabet_x10(void)

{
  int i = 0;
  char ch;

  while (i++ = 10)
    {
      for (ch = 'a'; letter <= 'z'; ch++)
	_putchar(ch);
      _putchar('\n');
    }
}
