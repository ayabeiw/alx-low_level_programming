#include "holberton.h"
/**
 * 
 *_abs - Computes the absolute value 
 *
 * @n: integer to be computed
 *
 * return: the absolute value
 */

int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
