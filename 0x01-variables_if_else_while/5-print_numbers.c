#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * Entry point 
 *
 * Return: 0 (Success)
 */
int main(void)
{
int w;

for (w = 0 ; w < 10 ; w++)
{
printf("%d", w);
}
putchar('\n');
return (0);
}
